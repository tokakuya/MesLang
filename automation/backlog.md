# 改善バックログ（小さく積む）

日次エージェントはここから拾い、終わったら消すか「done」へ移す。

## ready

- [ ] セリフなし見本の参考画像（任意・ADR 0004 どおり二次成果物）
- [ ] cafe-pose の参考画像（任意・ADR 0004）
- [ ] AI 向けガイドに字コンテ起こしの短いプロンプトを足す（木フォーカス向け）
- [ ] `toConteTable` の Markdown／CSV 整形は二次ツール側の候補（コアには入れない）

## 次の候補（木曜: AI 読み・生成）

- `docs/spec/04-ai-reading.md` にアニメ字コンテ向けの実務プロンプトを1つ（カット表・`?` 注意つき）
- 漫画参考画像を silent-panels / cafe-pose にも任意で足す

## parking（先送り・まだ手をつけない）

方針として「いまはやらない」と決めたものは、下の **wont（v0）** へ移す。ここは「将来ほしいかもしれないが優先度外」だけを置く。

- （空き）— 厳密な音響座標や GUI は wont へ移済み

## wont（v0 ではやらない / 採用しない）

哲学・ADR・用語集で見送った案。日次で再発明しないこと。

- VN 選択肢・分岐 DSL（コアには入れない。必要なら別プロファイル）
- 厳密な音響座標系（音声は左右・遠近メモまで）
- GUI エディタ必須化
- `::img=` HTML 風属性（ADR 0002）
- 属性キー名「ポーズ」（正は **姿勢**。ADR 0005）
- コアの `#` → セクション自動昇格（ADR 0003）
- 原稿への画像埋め込み必須（ADR 0004）
- `（ト書き）` / `【ト書き】` 行の自動変換（ツール固有のまま。コア／公式互換レイヤには入れない）

## done

- [x] MesLang v0 スケルトン（哲学・仕様・サンプル・参照パーサ・日次手順）
- [x] ADR 0003: `#` はト書き。柱書きセクション化はコア外（用語集も追加）
- [x] ADR 0004: 漫画参考画像は二次成果物。1ページ＝1枚でサンプル直下に表示
- [x] ADR 0005: ブラケット既定キー順（表情/姿勢/表情N）と用語統一（原稿≠二次ネーム）
- [x] 日曜振り返り（2026-07-26）: ADR 一覧、見送った案の用語集化、parking→wont 整理、コアの `○` 柱注記の明確化
- [x] ADR 0006: 旧 Mes 取り込み（`○`→`#` 互換レイヤ、ガイド・見本・`rewriteMesCompat`）
- [x] `$` / `!` 語彙ガイド + 音声見本の厚み（2026-07-26 夜）
- [x] Medo からセリフ文字数を集計（`countDialogueChars` / `npm run count`）
- [x] ADR 0007: プロファイル別かぎかっこキー表 + `bracket-keys` + CLI `--compat`（2026-07-27）
- [x] `$` / `:声質` / `#` の分け方を音声プロファイルに明記し、Mes 取り込み見本を追随（2026-07-27 午後）
- [x] ヘッダー `$key` vs 本文 `$音`、`$`/`!` 並び目安、`&` 語彙、属性キー数字始まり禁止（2026-07-27 夜）
- [x] 火曜: セリフなしコマ見本、`==`/`%` 失敗例、`:吹き出し` 対比、AI ネームプロンプト厚み（2026-07-28）
- [x] 火曜夜: 表情・姿勢実践見本（`cafe-pose.mes`）、`#`/`:表情`/`:姿勢` 分け方（2026-07-28）
- [x] 水曜: Medo schema `rawMark` 必須化、`validateMedo`、`?` 実例、字コンテ見本を厚く（2026-07-29）
- [x] 水曜夜: ADR 0008 絵コンテ二次出力、`toConteTable`、アニメ `^`/`&` 語彙（2026-07-29）
